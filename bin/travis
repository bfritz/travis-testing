#!/bin/bash -x

set -e

echo "/etc/sbt/jvmopts:"
cat /etc/sbt/jvmopts

echo
echo "/etc/sbt/sbtopts:"
cat /etc/sbt/sbtopts

SBT_CMD=$(which sbt)
echo "sbt is:"
echo $SBT_CMD
ls -al $SBT_CMD

echo "sbt sha1:"
sha1sum $SBT_CMD

sbt about
